	//Write a C program to compute the super-factorial of a given number n,sf(n)= 1!*2!*3!*…n!

#include <stdio.h>
int main()
{
    int  n,i,fact=1,j,sum=1;
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {       
       
           fact*=i;
        sum*=fact;
}
    printf("%d",sum);
    return 0;
}
