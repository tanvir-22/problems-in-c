#include<stdio.h>
int main()
{
    int n,rem,bin=0,i=1;
    printf("enter n:");
    scanf("%d",&n);
    while(n>0)
    {
        rem=n%2;
        n=n/2;
        bin=bin+(rem*i);
        i=i*10;
    }
    printf("%d",bin);
    return 0;
}
