#include<stdio.h>
#include<math.h>
int main()
{
    int n,d,i,sum=0;
    printf("enter n:");
    scanf("%d",&n);
    for(i=0; n!=0; i++)
    {
        d=n%10;
        sum=sum+d*pow(2,i);
        n=n/10;
    }
    printf("%d",sum);
    return 0;
}
